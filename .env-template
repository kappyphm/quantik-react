# Copy this file to .env, then replace the placeholder with your Vnstock API key.
# Never commit the .env file or put this key in VITE_ variables.
VNSTOCK_API_KEY=your-vnstock-api-key
# Generate a private random value for local administration. Never commit the real key.
# QUANTIK_ADMIN_KEY=replace-with-a-long-random-key
